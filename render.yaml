services:
  - type: web
    name: my-ai-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: API_KEY
        sync: false # User needs to set this manually in the dashboard or via a secret
      - key: PORT
        value: 5000
    plan: free
